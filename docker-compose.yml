services:
  duri-bugi:
    build: .
    container_name: duri-bugi
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - duri-data:/data
    environment:
      - TZ=Asia/Seoul
    env_file:
      - .env

volumes:
  duri-data:
