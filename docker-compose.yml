services:
  noti-ledger:
    build: .
    container_name: noti-ledger
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - noti-data:/data
    environment:
      - TZ=Asia/Seoul
    env_file:
      - .env

volumes:
  noti-data:
